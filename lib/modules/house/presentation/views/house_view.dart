import 'package:flutter/material.dart';

class HouseView extends StatelessWidget {
  const HouseView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('House View - To be implemented'),
    );
  }
}